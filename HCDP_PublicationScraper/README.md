To use this script, run url_extractor.py and enter url or local html file. Then enter beginning and ending substrings that are around the urls you want to download. The urls will be outputted to a file called output.txt. 

To download the files, run downloader.py. Enter the path of your output.txt file when prompted. Enter a directory you want to download to, otherwise it will default to a folder called downloads.
